# from django.apps import AppConfig
#
#
# class FiveStarsConfig(AppConfig):
#     name = 'five_stars'
#
#     def ready(self):
#         import five_stars.signals  # Ensure the signals are registered
